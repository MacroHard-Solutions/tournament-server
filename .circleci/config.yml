version: 2.1
jobs:
  build:
    working_directory: ~/repo
    docker:
      - image: cimg/node:16.13.2
    steps:
      - checkout
      - run:
          name: Update NPM
          command: "sudo npm install -g npm"
      - restore_cache:
          key: dependency-cache-{{ checksum "package-lock.json" }}
      - run:
          name: Install Dependencies
          command: npm install
      - save_cache:
          key: dependency-cache-{{ checksum "package-lock.json" }}
          paths:
            - ./node_modules
      - run:
          name: Test Suite
          command: npm test
      - run:
          name: Build React App
          command: npm run build
      - save_cache:
          key: app-build-cache-{{ .Branch }}
          paths:
            - ./build
      - run:
          name: Deploy to Netlify
          command: ./node_modules/.bin/netlify deploy --site 24efdb85-4420-4f05-af8f-1b0a588d00af --auth CHvWxHTcyb6hNPcrZxKnGImYPB5sqW8bLPSC8ZnbNKc
#orchestrate our job run sequence
workflows: 
  version: 2
  "build-deploy":
    jobs:
      - build:
          filters:
            branches:
              only:
                - interface
